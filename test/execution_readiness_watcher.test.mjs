import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";import{evaluateExecutionReadiness}from"../src/scanner/execution_readiness_watcher.mjs";
const good=()=>({account:{ok:true,status:"connected_readonly",runtime:{baseUrlHost:"paper-api.alpaca.markets",hasRuntimeKeys:true},account:{accountBlocked:false,tradingBlocked:false}},clock:{ok:true,status:"connected_readonly",marketClock:{isOpen:false}},pm2:[{name:"gemini-dry-scanner",status:"stopped"}],adapter:{enabled:true},env:{PAPER_AUTO_SUBMISSION_BOUNDARY_ENABLED:"1",PAPER_AUTO_ENTER_SUBMISSION_ENABLED:"1",PAPER_AUTO_EXIT_SUBMISSION_ENABLED:"1",LIVE_TRADING_ENABLED:"0"},lifecycle:null});
test("ready does not require market open",()=>{let r=evaluateExecutionReadiness(good(),0);assert.equal(r.ready,true);assert.equal(r.checks.marketOpen,false);assert.equal(r.safety.orderPlacementAllowed,false)});
test("fails closed on disabled enter",()=>{let i=good();i.env.PAPER_AUTO_ENTER_SUBMISSION_ENABLED="0";assert.equal(evaluateExecutionReadiness(i).ready,false)});
test("fails closed on live or ambiguous lifecycle",()=>{let i=good();i.env.LIVE_TRADING_ENABLED="1";i.lifecycle={state:"ENTER_UNKNOWN"};let r=evaluateExecutionReadiness(i);assert.equal(r.ready,false);assert.ok(r.blockers.includes("liveDisabled"));assert.ok(r.blockers.includes("lifecycleNeedsReview"))});
test("runtime has no submit path",()=>assert.doesNotMatch(fs.readFileSync("scripts/run_execution_readiness_watcher.mjs","utf8"),/submitPaperAutoOrder|submitPaperOrder\s*\(|\/v2\/orders|method:\s*["']POST/));
