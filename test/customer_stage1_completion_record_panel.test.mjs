import test from "node:test";
import assert from "node:assert/strict";
import { buildCustomerStage1CompletionRecordPanel, renderCustomerStage1CompletionRecordPanelHtml } from "../src/scanner/customer_stage1_completion_record_panel.mjs";
const completeStatus={tracker:{symbol:"TEST",baselineObserved:true,enterDetected:true,enterReconciled:true,monitoringStarted:true,exitDetected:true,exitReconciled:true,roundTripClosed:true,restartRecoveryVerified:true,duplicateProtectionVerified:true,mechanicalSuccess:true,baselineObservedAt:"2026-08-03T13:30:00.000Z",enterDetectedAt:"2026-08-03T13:35:00.000Z",exitDetectedAt:"2026-08-03T14:15:00.000Z"},promotionProof:{mechanicalSuccess:true,evidenceId:"abcdef0123456789abcdef01",completedAt:"2026-08-03T14:16:00.000Z"}};
test("remains hidden until complete promotion-grade evidence exists",()=>{const p=buildCustomerStage1CompletionRecordPanel({status:{tracker:{mechanicalSuccess:false},promotionProof:{}}});assert.equal(p.visible,false);assert.equal(renderCustomerStage1CompletionRecordPanelHtml(p),"")});
test("renders immutable read-only Stage 1 completion record",()=>{const p=buildCustomerStage1CompletionRecordPanel({status:completeStatus});const h=renderCustomerStage1CompletionRecordPanelHtml(p);assert.equal(p.visible,true);assert.match(h,/Manual paper round trip mechanically proven/);assert.match(h,/Evidence ID/);assert.match(h,/keeps Stage 2 locked/);assert.doesNotMatch(h,/<form|type="submit"|automatic stage promotion/i);assert.equal(p.safety.orderPlacementAllowed,false);assert.equal(p.safety.stage2Locked,true);assert.equal(p.safety.stage3Locked,true)});
